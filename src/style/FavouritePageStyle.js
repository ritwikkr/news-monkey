import styled from "styled-components";

const Wrapper = styled.div`
  .fav-content {
    .title {
      width: 70%;
      margin: 20px auto 0;
      text-align: center;
    }
  }
`;

export default Wrapper;
